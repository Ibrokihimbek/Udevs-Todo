import 'package:flutter/material.dart';
import 'package:udevs_to_do/widgets/task_remainder_widget.dart';

class HomePage extends StatelessWidget {
  const HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: GlobalAppBar(),
      body: Column(
        children: [],
      ),
    );
  }
}
